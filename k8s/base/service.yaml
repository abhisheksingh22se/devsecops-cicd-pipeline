apiVersion: v1
kind: Service
metadata:
  name: devsecops-app-svc
  labels:
    app: devsecops-app
spec:
  selector:
    app: devsecops-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
